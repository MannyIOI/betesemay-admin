import { PacmanLoader as pl} from 'react-spinners'
import styled from "styled-components";

export const PacmanLoader = styled(pl)`
    display: block;
    margin: 0 auto;
    border-color: blue;
    background: black;
    position: absolute;
`